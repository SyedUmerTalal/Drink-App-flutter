import 'package:flutter/material.dart';

class AppColors {
  //Color used in borders and linear gradient and segmented control as well as
  static const Color BORDER_YELLOW = Color(0xFFF7971D);
  //Color used in most of the test and the confirmation dialog bottom button color
  static const Color DARK_AMBER = Color(0xFFF7971D);
  //Used in  linear gradient
  static const Color DARK_BROWN = Color(0xFF582C05);
// it is used in linear gradient of snacks bar as raised button and dropdown field background color
  static const Color TEXT_YELLOW = Color(0xFFD58635);
  // it is used as a divider color in dialogs
  static const Color ORANGE = Color(0xFFF3630E);
  //Text field border color when it is enabled but not selected
  static const Color GOLDEN = Color(0xFF9F7A52);
  //Mostly used in circles in showing item count and Circle in map the cluster circle and edit profile cirlce
  static const Color CIRCLE_YELLOW = Color(0xFFDE8C18);
  //only used in credit card background
  static const Color CREDITCARD_BACKGROUND_COLOR = Color(0xFFF7971E);
  //only used for showing money text
  static const Color MONEY_TEXT_COLOR = Color(0xFFF7971D);
  //only used for selection on credit card
  static const Color LIGHT_GREEN = Color(0xFF6fb512);
  //only used for bottom sheet
  static const Color DARK_GREY = Color(0xFF141414);
  //only used for product card border
  static const Color LIGHT_GREY = Color(0xFF6A6A6A);

  static const Color GREY = Color(0xFF262626);

  static const TEXT_GREY = Color(0xFF7D7D7D);
}
